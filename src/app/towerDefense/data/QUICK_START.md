# 🎮 快速新增關卡指南

## 方法一：手動編輯（推薦新手）

### 步驟 1：複製模板

打開 `level-template.json`，複製全部內容

### 步驟 2：修改內容

```json
{
  "id": 6, // ⚠️ 改成下一個 ID
  "name": "你的關卡名稱", // 修改名稱
  "description": "關卡描述", // 修改描述
  "difficulty": "medium", // 選擇難度：easy/medium/hard/extreme
  "initialGold": 700, // 調整初始金幣
  "initialLives": 15, // 調整初始生命
  "waves": [
    // 配置波次
    {
      "waveNumber": 1,
      "enemies": [
        {
          "type": "goblin", // 敵人類型：slime/goblin/orc/dragon
          "count": 15, // 數量
          "interval": 1000 // 生成間隔（毫秒）
        }
      ],
      "delay": 8000 // 波次間隔（毫秒）
    }
  ],
  "rewards": {
    "gold": 300, // 通關獎勵金幣
    "stars": 3 // 星級
  }
}
```

### 步驟 3：添加到 levels.json

1. 打開 `levels.json`
2. 在 `levels` 陣列的最後一個關卡後面加上逗號 `,`
3. 貼上你的新關卡配置
4. 保存文件

## 方法二：使用生成器（推薦進階）

### 使用 LevelGenerator 工具

```typescript
import { LevelGenerator } from "./utils/LevelGenerator";
import { LevelDifficulty } from "./types";

// 生成新關卡
const newLevel = LevelGenerator.generateLevel(6, {
  name: "暗影森林",
  description: "黑暗中潛伏著危險的敵人",
  difficulty: LevelDifficulty.MEDIUM,
  waveCount: 4, // 可選，默認 3 波
});

// 輸出 JSON
console.log(JSON.stringify(newLevel, null, 2));
```

### 運行生成器

```bash
# 在專案根目錄執行
npx ts-node src/app/towerDefense/utils/LevelGenerator.ts
```

## 快速參考

### 難度推薦設定

| 難度    | 初始金幣 | 初始生命 | 主要敵人       | 波數 |
| ------- | -------- | -------- | -------------- | ---- |
| easy    | 500-700  | 20-25    | 史萊姆、哥布林 | 2-3  |
| medium  | 700-900  | 15-20    | 哥布林、獸人   | 3-4  |
| hard    | 900-1200 | 12-15    | 獸人、飛龍     | 4-5  |
| extreme | 1200+    | 8-12     | 大量飛龍       | 5+   |

### 敵人數據

| 類型   | 血量 | 速度 | 獎勵 | 建議間隔    |
| ------ | ---- | ---- | ---- | ----------- |
| slime  | 100  | 50   | 10   | 500-1000ms  |
| goblin | 200  | 70   | 20   | 800-1500ms  |
| orc    | 400  | 40   | 40   | 1200-2000ms |
| dragon | 1000 | 60   | 100  | 2000-5000ms |

### 常見配置範例

#### 簡單開場波

```json
{
  "waveNumber": 1,
  "enemies": [{ "type": "slime", "count": 10, "interval": 1000 }],
  "delay": 5000
}
```

#### 混合波次

```json
{
  "waveNumber": 2,
  "enemies": [
    { "type": "slime", "count": 15, "interval": 800 },
    { "type": "goblin", "count": 8, "interval": 1500, "delay": 5000 }
  ],
  "delay": 10000
}
```

#### Boss 波

```json
{
  "waveNumber": 5,
  "enemies": [
    { "type": "goblin", "count": 20, "interval": 700 },
    { "type": "orc", "count": 10, "interval": 1500, "delay": 5000 },
    { "type": "dragon", "count": 5, "interval": 3000, "delay": 12000 }
  ],
  "delay": 20000
}
```

## ⚠️ 常見錯誤

1. **忘記更新 ID**

   - ❌ 使用重複的 ID
   - ✅ 確保每個關卡 ID 唯一

2. **JSON 格式錯誤**

   - ❌ 缺少逗號或括號
   - ✅ 使用 JSON 驗證工具檢查

3. **敵人類型拼寫錯誤**

   - ❌ `"type": "Slime"` 或 `"type": "slim"`
   - ✅ `"type": "slime"` （小寫）

4. **難度設定錯誤**
   - ❌ `"difficulty": "normal"`
   - ✅ `"difficulty": "easy"` 或 `"medium"`

## 🎯 測試你的關卡

1. 保存 `levels.json`
2. 重新啟動開發服務器
3. 在遊戲中測試新關卡
4. 根據測試結果調整數值

## 💡 設計技巧

1. **漸進式難度**：每波都比前一波稍難
2. **給予喘息時間**：適當的波次間隔讓玩家建造防禦
3. **混合敵人**：不同類型的敵人增加策略性
4. **使用延遲**：讓強敵在玩家放鬆時出現
5. **平衡資源**：確保玩家有足夠資源但需要策略

## 📞 需要幫助？

查看詳細文檔：`data/README.md`
